- job-template:
    name: mcp-k8s-test-unit
    description: |
      Kubernetes unit test
    node: k8s
    concurrent: true
    parameters:
      - string:
          name: GERRIT_REFSPEC
          default: 'refs/heads/release-1.3'
          description: 'Gerrit refspec for manual building'
    builders:
      - inject:
          properties-content: |
            COVERAGE=n
            DOCKER_IMAGE={k8s-tests-registry}/k8s-tests-images/k8s-tests-unit:latest
            GERRIT_URL={gerrit-url}
            GIT_K8S_CACHE_DIR=/home/jenkins/kubernetes
      - shell:
          !include-raw-escape: builders/mcp-create-git-cache.sh
      - shell:
          !include-raw-escape: builders/mcp-k8s-test.sh
    wrappers:
      - workspace-cleanup
      - ssh-agent-credentials:
          users:
            - 'mcp-ci-gerrit'
    triggers:
      - gerrit:
          trigger-on:
            - patchset-created-event
            - comment-added-contains-event:
                comment-contains-value: '(recheck|reverify)'
          projects:
            - project-compare-type: PLAIN
              project-pattern: kubernetes/kubernetes
              branches:
                - branch-compare-type: ANT
                  branch-pattern: '**'
          custom-url: '* $JOB_NAME $BUILD_URL'
    publishers:
      - archive:
          allow-empty: true
          artifacts: '_artifacts/*'
          latest-only: false
      - junit:
          results: '_artifacts/**.xml'
      - post-tasks:
        - matches:
          - log-text: '.'
          escalate-status: true
          script: |
            sudo chown -R jenkins:jenkins "${{WORKSPACE}}"
